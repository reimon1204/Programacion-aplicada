#Ejemplo 1, asi obtenemos el tipo de datos

import numpy as np
arr = np.array([1, 2, 3, 4])
print(arr.dtype)

#Ejemplo 2, obtenemos el tipo de datos de una matriz con cadenas

import numpy as np
arr = np.array(['apple', 'banana', 'cherry'])
print(arr.dtype)
